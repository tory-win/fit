# 입핏 에이전트 작업 규칙

이 저장소는 입핏의 코드 정본이다. 제품 코드는 `app/`에 있다.

## 작업 경계

- `.env`, API 키, 광고·제휴 실계정 값, 사용자 사진, 기기 식별자, 로컬 터널 주소를 읽어 출력하거나 커밋하지 않는다.
- 실제 사용자 데이터와 실행 trace는 저장소 밖에 둔다. 테스트는 합성 데이터만 사용한다.
- 모델 출력은 제안일 뿐이다. 경로·명령·테스트·배포 대상은 결정적 코드와 사람이 검증한다.
- 운영 Docker, Funnel, iPhone 설치, 외부 공개, 유료 이미지 생성은 별도 운영 승인과 영수증 없이는 실행하지 않는다.
- 이 저장소 밖의 실기기 연결 운영 사본은 명시적 승격 전에는 수정하지 않는다.

## 검증

```bash
cd app
npm ci
npm run typecheck
npm test
npm run lint
npm run build
npm run build:real
```

변경 보고에는 수정한 파일, 실행한 검증, 실패·미검증 경계, 산출물 위치를 남긴다.
